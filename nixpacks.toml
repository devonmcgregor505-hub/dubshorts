[phases.setup]
nixPkgs = ["nodejs_22", "ffmpeg", "dejavu_fonts", "python311"]

[phases.install]
cmds = [
  "npm ci",
  "python3 -m pip install opencv-python-headless numpy Pillow --break-system-packages"
]

[start]
cmd = "npm start"
